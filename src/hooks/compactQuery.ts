/** 좁은 화면 또는 터치 태블릿급 — layout 인라인 스크립트·useCompactUi 공유.
 *  세분 프로파일(phone/tablet/desktop/desktop-wide)은 `@/hooks/deviceQueries` 참고. */
export const COMPACT_QUERY =
  "(max-width: 768px), ((pointer: coarse) and (max-width: 1024px))";
